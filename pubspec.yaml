name: smartpay_ai
description: SmartPay AI - E Wallet App
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  crypto: ^3.0.7
  shared_preferences: ^2.5.5
  local_auth: ^3.0.1
  sqflite: ^2.4.2+1
  path: ^1.9.1
  http: ^1.6.0
  sensors_plus: ^7.0.0
  image_picker: ^1.2.2
  geolocator: ^14.0.2
  google_maps_flutter: ^2.17.0
  geocoding: ^4.0.0
  url_launcher: ^6.3.2
  flutter_local_notifications: 19.5.0
  flutter_map: ^8.3.0
  latlong2: ^0.9.1
  qr_flutter: ^4.1.0
  mobile_scanner: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
